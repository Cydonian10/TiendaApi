{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Depurar en Terminal Externa",
      "type": "node", // o cppdbg, node, etc. según tu entorno
      "request": "launch",
      "program": "${file}",
      "console": "externalTerminal"
    }
  ]
}
